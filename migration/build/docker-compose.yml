version: '3'

services:
  golang-migration:
    container_name: golang-migration
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ../:/app
    environment:
      MYSQL_USER: root
      MYSQL_PASSWORD: root
      MYSQL_HOST: mysql
      MYSQL_PORT: 3306
      DATABASE_NAME: wallet
